[gd_resource type="ShaderMaterial" load_steps=6 format=2]

[ext_resource path="res://assets/overworld/map/tiles/shoreline/bot/bot1.png" type="Texture" id=1]
[ext_resource path="res://assets/overworld/map/tiles/shoreline/bot/bot2.png" type="Texture" id=2]
[ext_resource path="res://assets/overworld/map/tiles/shoreline/bot/bot3.png" type="Texture" id=3]
[ext_resource path="res://assets/overworld/map/tiles/shoreline/bot/bot4.png" type="Texture" id=4]

[sub_resource type="Shader" id=1]

code = "shader_type canvas_item;

uniform sampler2D frame1;
uniform sampler2D frame2;
uniform sampler2D frame3;
uniform sampler2D frame4;

void fragment() {
	float frames = 4.0;
	float frameLength = 0.2;
	
	float time = fract(TIME/(frames*frameLength))*frames;
	
	if (time < 1.0) {
		COLOR = texture(frame1, UV);
	} else{ 
		if (time < 2.0) {
			COLOR = texture(frame2, UV);
		} else{ 
			if (time < 3.0) {
				COLOR = texture(frame3, UV);
			} else{ 
				if (time < 4.0) {
					COLOR = texture(frame4, UV);
				}
			}
		}
	}
}"

[resource]

render_priority = 0
shader = SubResource( 1 )
shader_param/frame1 = ExtResource( 1 )
shader_param/frame2 = ExtResource( 2 )
shader_param/frame3 = ExtResource( 3 )
shader_param/frame4 = ExtResource( 4 )
_sections_unfolded = [ "shader_param" ]

