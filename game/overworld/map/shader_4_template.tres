[gd_resource type="CanvasItemMaterial" load_steps=6 format=1]

[ext_resource path="res://assets/overworld/map/tiles/shoreline/shoreline/shoreline1.png" type="Texture" id=1]
[ext_resource path="res://assets/overworld/map/tiles/shoreline/shoreline/shoreline2.png" type="Texture" id=2]
[ext_resource path="res://assets/overworld/map/tiles/shoreline/shoreline/shoreline3.png" type="Texture" id=3]
[ext_resource path="res://assets/overworld/map/tiles/shoreline/shoreline/shoreline4.png" type="Texture" id=4]

[sub_resource type="CanvasItemShader" id=1]

_code = {
"fragment": "uniform texture frame1;
uniform texture frame2;
uniform texture frame3;
uniform texture frame4;
float frames = 4.0;
float frameLength = 0.2;

float time = fract(TIME/(frames*frameLength))*frames;
color col;

if (time < 1) {
	col = tex(frame1, UV);
} else{ 
	if (time < 2) {
		col = tex(frame2, UV);
	} else{ 
		if (time < 3) {
			col = tex(frame3, UV);
		} else{ 
			if (time < 4) {
				col = tex(frame4, UV);
			}
		}
	}
}

COLOR = col;",
"fragment_ofs": 0,
"light": "",
"light_ofs": 0,
"vertex": "",
"vertex_ofs": 0
}

[resource]

shader/shader = SubResource( 1 )
shader/shading_mode = 0
shader_param/frame1 = ExtResource( 1 )
shader_param/frame2 = ExtResource( 2 )
shader_param/frame3 = ExtResource( 3 )
shader_param/frame4 = ExtResource( 4 )

